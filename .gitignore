# Binaries
bin/
/meshsat-hub
*.exe

# Go
vendor/

# Node
node_modules/
web/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment
.env
*.env.local

# Claude Code (local-only, never commit)
CLAUDE.md
.claude/

# Data
*.db
*.db-journal
*.db-wal
data/

# OWASP scan reports (generated)
test/owasp/reports/
owasp-reports/

# Python RNS compatibility tests (diagnostic tools, not CI)
test/rns_compat/

# Docker
docker-compose.override.yml

# Tor keys (sensitive)
tor-keys/

# OS
.DS_Store
Thumbs.db
meshsat-sim

# python bytecode from local script checks
__pycache__/
*.pyc

# Probe credentials. scratchpad/ scripts drive production; the one key they may
# be given is a Stripe TEST key, and it still never belongs in git.
scratchpad/.stripe-test-key
scratchpad/*.key
scratchpad/.env
