apiVersion: v1
kind: Service
metadata:
  name: stunnel
  namespace: meshsat-hub
  labels:
    app.kubernetes.io/name: stunnel
spec:
  selector:
    app.kubernetes.io/name: stunnel
  ports:
    - {name: reticulum, port: 4243, targetPort: reticulum}
