apiVersion: v1
kind: Service
metadata:
  name: basemap
  namespace: meshsat-hub
  labels:
    app.kubernetes.io/name: basemap
spec:
  selector:
    app.kubernetes.io/name: basemap
  ports:
    - {name: http, port: 80, targetPort: http}
